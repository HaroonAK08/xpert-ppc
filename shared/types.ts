/**
 * Icons are referenced by name so content stays plain data — the frontend
 * maps these strings onto lucide-react components, and the backend can store
 * them in MongoDB untouched.
 */
export type IconName = string;
